---
const { skill } = Astro.props;
---

<div class="flex flex-col items-center justify-center bg-gray-800 p-6 rounded-lg shadow-md text-center bg-gray-800/50">
    <div class="w-24 h-24 mb-4">
        <img src={`/images/skills/${skill.icon}`} alt={skill.name} class="w-full h-full object-cover" />
    </div>
    <h3 class="text-2xl font-bold mb-2 text-gray-200">{skill.name}</h3>
    <p class="text-gray-300 text-sm">{skill.description}</p>
</div>
